<Window x:Class="AutoMarket.View.EditProductView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AutoMarket.View"
        mc:Ignorable="d"
        Title="EditProductView" Height="450" Width="800"
        Name="EditProductWindow">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" HorizontalAlignment="Center"
            VerticalAlignment="Center" Orientation="Vertical"
            Width="200">
            <TextBlock Text="Новая категория"></TextBlock>
            <ComboBox ItemsSource="{Binding AllCategories}" 
                      SelectedItem="{Binding CategoryProduct}"
                      DisplayMemberPath="Name"/>
            <TextBlock Text="Новый производитель"></TextBlock>
            <ComboBox ItemsSource="{Binding AllManufacturers}" 
                      SelectedItem="{Binding ManufacturerProduct}"
                      DisplayMemberPath="Name"/>
            <TextBlock Text="Новый продукт"></TextBlock>
            <TextBox Text="{Binding ProductName}"></TextBox>
            <TextBlock Text="Новая цена"></TextBlock>
            <TextBox Text="{Binding PriceProduct}"></TextBox>
            <TextBlock Text="Новое описание"></TextBlock>
            <TextBox Text="{Binding descriptionProduct}"></TextBox>
        </StackPanel>
        <Button Grid.Row="1"
                Command="{Binding EditProduct}"
                CommandParameter="{Binding ElementName=EditProductWindow}"
                Content="Сохранить изменения"/> 
    </Grid>
</Window>
