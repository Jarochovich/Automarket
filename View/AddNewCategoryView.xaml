<Window x:Class="AutoMarket.View.AddNewCategoryView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AutoMarket.View"
        mc:Ignorable="d"
        Title="AddNewCategoryView" Height="300" Width="500"
        Name="AddNewCategoryV">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" HorizontalAlignment="Center"
            VerticalAlignment="Center" Orientation="Vertical"
            Width="200">
            
            <TextBlock Text="Категория" FontSize="16"></TextBlock>
            <TextBox Name="CategoryTextBox" Text="{Binding CategoryName}" FontSize="14"></TextBox>

         

        </StackPanel>
        <Button Grid.Row="1" 
                Margin="0 0 0 -18"
                Content="Добавить новую категорию" 
                Command="{Binding AddNewCategory}" 
                CommandParameter="{Binding ElementName=AddNewCategoryV}" />
    </Grid>
</Window>
