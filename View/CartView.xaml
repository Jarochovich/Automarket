<Window x:Class="AutoMarket.View.CartView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Корзина" Height="450" Width="800"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <!-- Список товаров в корзине -->
        <ListView ItemsSource="{Binding CartProducts}" Margin="10">
            <ListView.View>
                <GridView>
                    <!-- Колонки для отображения товаров -->
                    <GridViewColumn Header="Название товара" DisplayMemberBinding="{Binding Name}" Width="200"/>
                    <GridViewColumn Header="Цена" DisplayMemberBinding="{Binding Price}" Width="100"/>
                    <GridViewColumn Header="Количество" DisplayMemberBinding="{Binding CountItem}" Width="100"/>

                    <!-- Удалить товар -->
                    <GridViewColumn Header="Удалить">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Удалить" Command="{Binding DataContext.RemoveCommand}" CommandParameter="{Binding}" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <!-- Общая сумма товаров в корзине -->
        <TextBlock Text="{Binding TotalPrice, StringFormat='Итого: {0} BYN'}"
                   VerticalAlignment="Bottom"
                   HorizontalAlignment="Right"
                   Margin="0,10,10,10"
                   FontSize="16"
                   FontWeight="Bold" />

        <!-- Кнопка оплаты -->
        <Button VerticalAlignment="Bottom"
                HorizontalAlignment="Left"
                Margin="0,10,10,10"
                FontSize="16"
                FontWeight="Bold" 
                Content="Оплатить"
                Command="{Binding PayCommand}"/>
    </Grid>
</Window>
