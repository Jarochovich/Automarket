<Window x:Class="AutoMarket.View.AddNewUserView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AutoMarket.View"
        mc:Ignorable="d"
         xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="AddNewUserView" Height="300" Width="500"
        Name="AddNewUserWindow">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" HorizontalAlignment="Center"
                    VerticalAlignment="Center" Orientation="Vertical"
                    Width="200">
            <TextBlock Text="Login"></TextBlock>
            <TextBox Name="NameUser" Text="{Binding UserName}"></TextBox>
            <TextBlock Text="Password"></TextBlock>
            <PasswordBox PasswordChanged="PasswordUser_OnPasswordChanged" Name="PasswordUser" />
            <TextBlock Text="Money"></TextBlock>
            <TextBox Name="MoneyUser" Text="{Binding UserMoney}"></TextBox>

        </StackPanel>
        <Button Grid.Row="1" 
                Command="{Binding AddNewUser}"
                CommandParameter="{Binding ElementName=AddNewUserWindow}"
                >Add new User</Button>
    </Grid>
</Window>
